//hitung total data
const totalData = document.querySelectorAll('.btn-copy');
const containerTotalData = document.querySelector('.data-quantity');

function hitungData(){
	setTimeout(function(){
		const totalData = document.querySelectorAll('.btn-copy');
		console.log(totalData.length);
	},1000);
}
